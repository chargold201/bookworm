<h1><%= @book.title %></h1>

<p>Author: <%= @book.author %></p>
<p>Genre: <%= @book.genre.name %></p>

<p><%= link_to "Edit this book", edit_book_path(@book) %></p>
<p><%= link_to "Delete this book", book_path(@book), method: :delete, data: { confirm: "Are you sure?"} %></p>

<p><%= link_to "Write a review for this book", new_book_review_path(@book) %></p>
<p><%= link_to "View all reviews for this book", book_reviews_path(@book) %></p>
